# Scripts

The verified Bash baseline script will be stored here when Sat builds it.

Scripts must be public-safe, use readable output labels and avoid usernames,
hostnames, IP addresses, credentials and keys.

