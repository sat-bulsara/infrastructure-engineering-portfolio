# Project 01 Exit Quiz

- Status: Deferred into Project 02 for interleaved retrieval
- Conditions: No notes; one rapid batch requested for closeout
- Help before attempt: None

## Questions

1. Which sequence best describes what happens when a command is entered in a
   terminal: terminal to Bash to kernel, kernel to terminal to Bash, or Bash to
   terminal to kernel?
2. What value will `cpu_cores=$(nproc)` store on the current VM?
3. What is wrong with `$cpu_cores=$(nproc)`?
4. Write one Bash line that prints the value of `architecture` with the label
   `Architecture:`.
5. Why was `rm -- controlled-test-file.bin` safer than a broad wildcard?
6. On an unfamiliar Linux host, name the first two pieces of context you would
   verify before making changes.

## Review record

Answers and any repair evidence belong in `PROGRESS.md` or a learning record,
not in this unattempted quiz file.
