# Operational Scripts

Azure CLI and Azure PowerShell verification will be recorded here after the
portal workflow is understood. Scripts must use parameters, avoid embedded
identifiers and secrets, and verify results independently.
