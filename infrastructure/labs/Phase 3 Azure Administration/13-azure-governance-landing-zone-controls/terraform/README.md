# Terraform Scope Decision

Terraform was deliberately deferred after the portal, Azure CLI, Bash and
PowerShell governance workflow was understood and verified.

The next networking project will use Terraform as a changed deployment
variation. This keeps the course moving while still repeating infrastructure as
code against a useful new resource model.
